name: WPWW CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: wpww-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v7
        with:
          node-version: 22

      - run: npm install --package-lock-only
      - run: node --check server.js
      - run: python3 -m py_compile verify_all.py tools/auto_heal.py tools/validate_wpww.py tools/mission_test.py tools/mission_control.py tools/incident_engine.py tools/report_export.py tools/module_inventory.py
      - run: python3 tools/auto_heal.py
      - run: python3 tools/module_inventory.py
      - run: python3 tools/validate_wpww.py
      - run: test -s public/index.html
      - run: test -s public/reaction.js
      - run: test -s Dockerfile
      - run: test -s docker-compose.yml
      - run: test -s verify_all.py
      - run: test -s tools/mission_test.py

  container-smoke:
    runs-on: ubuntu-24.04
    needs: validate
    steps:
      - uses: actions/checkout@v4
      - name: Build and start WPWW
        run: docker compose up --build -d
      - name: Wait for HTTP endpoint
        run: |
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:8080/healthz >/dev/null; then exit 0; fi
            sleep 2
          done
          docker compose ps
          docker compose logs --no-color
          exit 1
      - name: Run complete verification
        run: python3 verify_all.py
      - name: Run Mission Control verification
        run: python3 tools/mission_test.py
      - name: Run module inventory
        run: python3 tools/module_inventory.py
      - name: Diagnostics on failure
        if: failure()
        run: |
          docker compose ps
          docker compose logs --no-color
      - name: Teardown
        if: always()
        run: docker compose down -v
