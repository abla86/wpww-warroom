# WPWW War Room

Standalone live operations dashboard.

## Run

docker compose up --build

Open:

http://localhost:8080

The dashboard never invents healthy state. Unavailable upstream services are displayed as UNKNOWN/OFFLINE.
